[project]
name = "usanity"
version = "0.1.0"
description = "MicroPython client for Sanity.io (https://www.sanity.io)"
readme = "README.md"
requires-python = ">=3.13"

[project.optional-dependencies]
dev = [
    "black>=26.1.0",
]
